import {
  getRandomCoffee,
  getRandomCoffeeList,
  filterPropertyRandomCoffeeList,
} from "./coffeeFunctions";

/**
 * Random Data APIの/coffee/random_coffeeからデータを取得する
 * ドキュメント： https://random-data-api.com/documentation
 */

describe("getRandomCoffee", () => {
  /**
   * 単一のCoffeeオブジェクトを取得する
   */
});

describe("getRandomCoffeeList", () => {
  /**
   * 複数のCoffeeオブジェクトを取得する
   */
});

describe("filterPropertyRandomCoffeeList", () => {
  /**
   * 取得した複数のCoffeeから必要なプロパティだけを抜き出す
   */
});
